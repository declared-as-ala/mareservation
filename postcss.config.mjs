/**
 * PostCSS Configuration for Next.js + Tailwind CSS v4.
 *
 * Tailwind v4 ships its plugins as the `@tailwindcss/postcss` package which
 * handles compilation, vendor-prefixing, and CSS-nesting automatically.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
